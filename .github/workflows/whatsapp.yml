name: CADIIA WhatsApp Agent

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"  # backup a cada 10min caso pare

jobs:
  agente:
    runs-on: ubuntu-latest
    timeout-minutes: 355  # limite máximo (~6h)

    steps:
      - uses: actions/checkout@v4

      - name: Instalar dependências
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip
          pip install openai requests
          npm install node-fetch

      - name: Iniciar ciclo CADIIA (Z-API + GitHub)
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ZAPI_INSTANCE: ${{ secrets.ZAPI_INSTANCE }}
          ZAPI_TOKEN: ${{ secrets.ZAPI_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🟢 CADIIA iniciando ciclo contínuo..."
          node relay.js & python3 processar.py
